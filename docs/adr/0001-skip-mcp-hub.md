# Skip MCP Hub in the first sample

A reader sees two MCP proxies and cannot tell which one talks to GitHub. Hub only stops a one-hour session token from leaving the Devbox. Scalekit Virtual MCP already holds the GitHub OAuth token.

We omit Hub in v1. Claude Code in the Devbox calls Virtual MCP with the session token. Hub can be an optional later chapter.
